{
  "name": "dojo-manager",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "appwrite": "^11.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-hook-form": "^7.43.9",
    "react-scripts": "5.0.1",
    "victory": "^36.6.10",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "copy-env": "cp src/environments/environment.$NODE_ENV.js src/environments/environment.js",
    "start:local": "NODE_ENV=local npm run start",
    "start:prod": "NODE_ENV=prod npm run start",
    "start": "npm run copy-env && react-scripts start",
    "build": "NODE_ENV=prod npm run copy-env && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
